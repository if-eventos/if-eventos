import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    Container: {
        marginTop: 55,
        flexDirection: "row",
        gap: 10
    },
    pageName: {
        fontWeight: 'bold',
        fontFamily: 'Roboto',
        fontSize: 16
    },
    descricao: {
        fontFamily: 'Roboto',
        fontSize: 12
    }
});